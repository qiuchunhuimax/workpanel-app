ProgressBar shows completion toward a milestone or task set — always ochre fill, never the terracotta accent (that color is reserved for actions).

```jsx
<ProgressBar label="阶段一 · 需求对齐" value={62} />
```

6px track height, pill-capped ends. Pair with a Badge for the discrete status word; ProgressBar itself communicates the continuous quantity.
